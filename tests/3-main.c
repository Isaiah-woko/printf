#include "../main.h"

/**
 * main - entry point
 * Desciption: main file to handle 'u' 'o' 'x' 'X'
 *
 * Return: 0
 */

int main(void)
{
	unsigned int ui;

	ui = (unsigned int)INT_MAX + 1024;
	 _printf("Unsigned:[%u]\n", ui);
	 printf("Unsigned:[%u]\n", ui);
}
